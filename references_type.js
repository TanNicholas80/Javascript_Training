// 1. object 
const user = {
    userid : 101,
    username : "john_doe",
}

console.log(user)

// 2. array
const pl = ['js', 'golang', 'pythone', 'java']

console.log(pl)

// 3. function
const sayHello = function(nama) {
    console.log(`Hello ${nama} !`)
}

sayHello("Nicholas")

// 4. date
const now = new Date()

console.log(now)